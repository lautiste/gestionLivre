{% extends 'base.html.twig' %}

{% block content %}
    <h1>List of Books</h1>
    <ul>
        {% for book in books %}
            <li>{{ book.title }}</li>
        {% endfor %}
    </ul>
{% endblock %}
